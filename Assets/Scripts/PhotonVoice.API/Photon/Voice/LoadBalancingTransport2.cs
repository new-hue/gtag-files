using ExitGames.Client.Photon;

namespace Photon.Voice
{
	public class LoadBalancingTransport2 : LoadBalancingTransport
	{
		public LoadBalancingTransport2(ILogger logger, ConnectionProtocol connectionProtocol) : base(default(ILogger), default(ConnectionProtocol))
		{
		}

	}
}
