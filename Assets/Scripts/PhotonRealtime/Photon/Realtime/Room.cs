using ExitGames.Client.Photon;

namespace Photon.Realtime
{
	public class Room : RoomInfo
	{
		public Room(string roomName, RoomOptions options, bool isOffline) : base(default(string), default(Hashtable))
		{
		}

	}
}
